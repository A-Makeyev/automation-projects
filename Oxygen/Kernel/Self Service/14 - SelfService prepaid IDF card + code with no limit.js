po.updateKarnelDBSettings('self service', false)
po.initService(
    '14 - Data for SelfService prepaid IDF card + code with no limit.js',
    '14 - Expected Result for SelfService prepaid IDF card + code with no limit.js'
)
